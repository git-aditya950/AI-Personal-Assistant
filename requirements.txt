# Core dependencies for LLM-powered Voice Assistant
openai>=1.12.0
speech-recognition==3.10.0
edge-tts>=6.1.9
pygame>=2.6.0

# Optional but recommended
python-dotenv>=1.0.0
aiofiles>=23.0.0

# For PyAudio (microphone support)
# Windows (Python 3.13+): pip install PyAudio (should work directly)
# Windows (Python 3.12-): pip install pipwin && pipwin install pyaudio (pipwin doesn't support 3.13)
# Linux: sudo apt-get install portaudio19-dev && pip install pyaudio
# macOS: brew install portaudio && pip install pyaudio
PyAudio>=0.2.13
